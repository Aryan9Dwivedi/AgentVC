[project]
name = "agentvc-generator"
version = "0.1.0"
description = "Raw source ingestion generator for biotech and biopharma VC intelligence."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[project.scripts]
agentvc-generator = "agentvc_generator.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
